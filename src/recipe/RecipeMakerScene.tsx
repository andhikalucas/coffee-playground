import { PersonaTitle } from '../components/persona/PersonaTitle'

/** placeholder — replaced by the real maker in M5 */
export function RecipeMakerScene() {
  return (
    <div style={{ display: 'grid', placeItems: 'center', height: '100%' }}>
      <PersonaTitle text="make a recipe" />
    </div>
  )
}
