import { PersonaTitle } from '../components/persona/PersonaTitle'

/** placeholder — replaced by the real playground in M3 */
export function PlaygroundScene() {
  return (
    <div style={{ display: 'grid', placeItems: 'center', height: '100%' }}>
      <PersonaTitle text="playground" />
    </div>
  )
}
